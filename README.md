# 维保收银系统

基于 Vue 3 + TypeScript + Element Plus 的后台管理系统，参考 Art Design Pro 风格设计。

## 技术栈

- Vue 3.5 + Composition API
- TypeScript 5.6
- Element Plus 2.11
- Vue Router 4.5
- Pinia 3.0 + 持久化
- Vue I18n 9.14
- Axios
- Sass
- Vite 7

## 功能特性

### � 屏认证系统
- 登录/注册/忘记密码页面
- JWT Token 认证
- 路由守卫（未登录自动跳转）
- 登录状态持久化

### � 生主题系统
- 亮色/暗色/跟随系统 三种模式
- 7 种预设主题色
- 主题切换圆形扩散动画（View Transition API）
- 设置自动持久化

### 📐 布局系统
- 4 种菜单布局（垂直/水平/混合/双列）
- 3 种菜单风格
- 菜单折叠/展开
- 响应式设计

### ⚙️ 设置面板
- 主题风格切换
- 菜单布局切换
- 菜单风格切换
- 系统主题色选择
- 盒子样式（边框/阴影）
- 容器宽度（流式/定宽）
- 基础配置（多标签栏、面包屑、进度条等）

### 🌐 国际化
- 中文/英文切换
- Element Plus 组件国际化

### 🔧 HTTP 请求
- Axios 封装
- 请求/响应拦截器
- 统一错误处理
- 401 自动登出

## 快速开始

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build
```

## 项目结构

```
src/
├── api/                    # API 接口
│   └── auth.ts            # 认证接口
├── components/            # 公共组件
│   ├── core/             # 核心组件
│   │   ├── ArtDragVerify.vue   # 拖拽验证
│   │   ├── ArtLogo.vue         # Logo
│   │   └── SettingsPanel.vue   # 设置面板
│   └── ArtSvgIcon.vue    # SVG 图标
├── config/               # 配置文件
├── enums/                # 枚举定义
├── layout/               # 布局组件
│   ├── index.vue        # 主布局
│   └── components/      # 布局子组件
├── locales/             # 国际化
│   └── langs/
│       ├── zh.json      # 中文
│       └── en.json      # 英文
├── router/              # 路由配置
├── store/               # 状态管理
│   └── modules/
│       ├── user.ts      # 用户状态
│       └── setting.ts   # 设置状态
├── styles/              # 全局样式
├── utils/               # 工具函数
│   ├── http/           # HTTP 封装
│   ├── animation.ts    # 动画工具
│   └── theme.ts        # 主题工具
└── views/              # 页面视图
    ├── auth/           # 认证页面
    │   ├── login/     # 登录
    │   ├── register/  # 注册
    │   └── forget-password/
    ├── dashboard/     # 仪表盘
    └── ...
```

## 环境配置

API 地址在 `src/utils/http/index.ts` 中配置：

```typescript
const BASE_URL = 'http://192.168.98.1:11015'
```

## 接口说明

### 登录接口

```
POST /account/login
Content-Type: application/json

{
  "username": "admin",
  "password": "123456"
}
```

响应格式：

```json
{
  "code": 0,
  "message": "操作成功",
  "data": {
    "id": 1,
    "username": "admin",
    "token": "eyJ...",
    "secondary": "xxx"
  }
}
```

## 参考项目

[Art Design Pro](https://github.com/Daymychen/art-design-pro)
